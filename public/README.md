# Static assets

Files here are served from the site root (e.g. `public/calvin.png` → `/calvin.png`).

- `calvin.webp` — avatar shown on calendar bookings that include Calvin.
  A square image works best (it's rendered as a small circle). Until the file
  exists the avatar simply doesn't render.
