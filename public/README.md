# Static assets

Files here are served from the site root (e.g. `public/calvin.png` → `/calvin.png`).

- `calvin.png` — avatar shown on calendar bookings that include Calvin.
  Save the baby photo here with exactly this name. A square image works best
  (it's rendered as a small circle). Until the file exists the avatar simply
  doesn't render.
